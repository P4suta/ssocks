# SPDX-FileCopyrightText: 2026 ssocks contributors
# SPDX-License-Identifier: MIT OR Apache-2.0

name = "ssocks"
# x-release-please-start-version
version = "0.1.0"
# x-release-please-end
gleam = ">= 1.18.0 and < 2.0.0"
target = "erlang"
# `ssocks_codec` has always had this; without it here, `ssocks/internal/clock`
# and `ssocks/internal/upstream` are published API and appear in the generated
# documentation as though a caller were meant to use them.
internal_modules = ["ssocks/internal/*"]

description = "Shadowsocks for Gleam: a client, a SOCKS5 proxy, a server and a UDP relay, on Erlang"
licences = ["MIT", "Apache-2.0"]
repository = { type = "github", user = "P4suta", repo = "ssocks" }

[dependencies]
gleam_stdlib = ">= 0.44.0 and < 2.0.0"
gleam_erlang = ">= 1.3.0 and < 2.0.0"
gleam_otp = ">= 1.2.0 and < 2.0.0"
ssocks_codec = { path = "../ssocks_codec" }
glisten = ">= 9.0.0 and < 9.1.0"
mug = ">= 3.1.0 and < 3.2.0"
toss = ">= 1.0.0 and < 1.1.0"
# Direct because the UDP relay names `glip.IpAddress` in its own types: a client
# address is what the session table is keyed on, and toss reports one with every
# datagram.
glip = ">= 1.0.0 and < 1.1.0"

[dev_dependencies]
gleeunit = ">= 1.9.0 and < 1.12.0"
argv = ">= 1.0.0 and < 2.0.0"
